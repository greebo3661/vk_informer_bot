# 🗓️ VK Teams Vacation Bot

Бот для VK Teams, который принимает Excel-файл с графиком отпусков и отправляет напоминания о предстоящих отпусках сотрудников.

## Возможности

- 📎 Принимает Excel-файл с графиком отпусков (поддержка объединённых ячеек и нестандартных заголовков)
- 👥 Поддержка нескольких пользователей — у каждого чата своя база отпусков
- ⏰ Отправляет уведомления за настраиваемое количество дней до начала отпуска
- 📅 Показывает расписание ближайших 5 уведомлений
- 🔔 Позволяет изменить порог оповещения прямо из меню
- 📢 Отправляет уведомления в указанный чат/канал
- 🕘 Проверяет отпуска каждый день в 09:00

## Формат Excel-файла

Бот автоматически определяет структуру файла. Поддерживаемый формат:

| ФИО | Организация | Кол-во дней | (пусто) | Дата начала |
|-----|-------------|-------------|---------|-------------|
| Иванов Иван Иванович | ООО Компания | 14 | | 01.06.2026 |

Дата может быть в формате `ДД.ММ.ГГГГ` или как Excel-дата.

## Быстрый старт

### 1. Настройка `.env`

```env
VKT_BOT_TOKEN=ваш_токен_бота
VKT_BASE_URL=https://myteam.mail.ru/bot/v1
DATA_FILE=/data/vacation_data.json
FILE_PATH=/data/latest.xlsx
```

> **Для on-premise:** замените `VKT_BASE_URL` на адрес вашего сервера VK Teams, например `https://teams.company.com/bot/v1`

### 2. Запуск через Docker

```bash
docker compose up --build -d
```

### 3. Использование бота

1. Откройте чат с ботом и отправьте `/start`
2. Прикрепите Excel-файл с графиком отпусков
3. Введите количество дней для предупреждения (например: `7`)
4. Бот будет присылать напоминания каждый день в 09:00

## Кнопки меню

| Кнопка | Описание |
|--------|----------|
| 📊 **Статус** | Количество загруженных записей и текущие настройки |
| ℹ️ **Помощь** | Краткая справка по работе с ботом |
| 📅 **Расписание** | 5 ближайших дат отправки уведомлений |
| 🔔 **Уведомления** | Изменить порог оповещения (в днях) |
| 📢 **Установить этот чат** | Назначить текущий чат для получения уведомлений |

## Команды

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы |
| `/status` | Текущие настройки и количество записей |
| `/help` | Справка |

## Структура проекта

```
.
├── src/
│   └── main.py          # Основной код бота
├── data/                # Данные (создаётся автоматически)
│   ├── vacation_data.json
│   └── latest.xlsx
├── .env                 # Переменные окружения (не в git)
├── .env.example         # Пример конфигурации
├── Dockerfile
├── docker-compose.yml
└── requirements.txt
```

## Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VKT_BOT_TOKEN` | Токен бота (**обязательно**) | — |
| `VKT_BASE_URL` | URL API VK Teams | `https://myteam.mail.ru/bot/v1` |
| `DATA_FILE` | Путь к JSON-файлу данных | `/data/vacation_data.json` |
| `FILE_PATH` | Путь для временного xlsx | `/data/latest.xlsx` |

## Технологии

- [bot-python](https://github.com/mail-ru-im/bot-python) — официальный SDK VK Teams
- [pandas](https://pandas.pydata.org/) + [openpyxl](https://openpyxl.readthedocs.io/) — парсинг Excel
- Python 3.11, asyncio, Docker
